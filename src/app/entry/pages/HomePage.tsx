import { Hero } from '../components/Hero';
import { DefaultLayout } from '../layout/DefaultLayout';

export const HomePage = () => {
  return (
    <DefaultLayout>
      <Hero />
    </DefaultLayout>
  );
};
